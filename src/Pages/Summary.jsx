import React from "react";
import { selectedWords } from "../lib/selectedWords";

const Summary = () => {
  return (
    <div>
      <span className="flex flex-wrap">word</span>
    </div>
  );
};

export default Summary;
